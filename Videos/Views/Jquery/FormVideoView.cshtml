@model Videos.Models.ViewModel.VideoDataView
<script>
    $(function() {
        $("#modal_form_video").on("click", ".add_artista", function() {
            $.post('@Url.Action("AdicionarArtistaJquery", "Jquery")', { id: $("#ListaArtistas").val(), nome_artista: $("#ListaArtistas option:selected").text(), model: "@Model.GetType().Name" },
            function(resposta) {
                $("#lista_artistas").html(resposta);
            });
        });
        $("#modal_form_video").on("click", ".add_musica", function() {
            var element = $(this).closest('div').find("input[name=musica]");
            $.post('@Url.Action("AdicionarMusicaJquery", "Jquery")', { titulo_musica: $(this).closest('div').find("input[name=musica]").val(), model: "@Model.GetType().Name" },
            function(resposta) {
                $("#lista_musicas").html(resposta);
                element.val('');
            });
        });
        $("#modal_form_video").on("click", ".add_tag", function() {
            var element = $(this).closest('div').find("input[name=tag]");
            $.post('@Url.Action("AdicionarTagJquery", "Jquery")', { nome_tag: element.val(), model: "@Model.GetType().Name" },
            function(resposta) {
                $("#lista_tags").html(resposta);
                element.val('');
            });
        });
        $("#modal_form_video").on("click", ".salvar", function() {
            var array = $("#form_video").serializeArray();
            var json = {};

            jQuery.each(array, function() {
                json[this.name] = this.value || '';
            });

            $.ajax({
                url: '@Url.Action("SalvarVideoJquery", "Jquery")',
                type: 'POST',
                contentType: 'application/json',
                data: JSON.stringify(json),
                success: function (resposta) {
                    $("#modal_form_video").modal('hide');
                }
            });
        });

        $("#modal_form_video").on("click", ".atualizar_metadata", function() {
            $.post('@Url.Action("AtualizarMetadataJquery", "Jquery")', { id: $(this).closest('div').find("#Id").val() },
            function(resposta) {
                $("#video_metadata").html(resposta);
            });
        });

        $('#modal_form_video').on('show.bs.modal', function(e) {
            $('#select_artistas').tokenize2({
                searchFromStart: false,
                delimiter: ';', //,
                placeholder: 'Artista' //false
            });
            $('#select_musicas').tokenize2({
                searchFromStart: false,
                delimiter: ';', //,
                placeholder: 'Música' //false
            });
            $('#select_tags').tokenize2({
                searchFromStart: false,
                delimiter: ';', //,
                placeholder: 'Tag' //false
            });
        })

        $('#select_artistas').tokenize2({
            searchFromStart: false,
            delimiter: ';', //,
            placeholder: 'Artista' //false
        });
        $('#select_artistas').on('tokenize:tokens:add', function (e, value, text) {
            $.post('@Url.Action("AdicionarArtistaJquery", "Jquery")', { id: value, nome_artista: text, model: "@Model.GetType().Name" },
            function (resposta) {
                $("#lista_artistas").html(resposta);
            });
        });
        $('#select_artistas').on('tokenize:tokens:remove', function (e, value) {
            $.post('@Url.Action("RemoverArtistaJquery", "Jquery")', { id: value, model: "@Model.GetType().Name" },
            function (resposta) {
                $("#lista_artistas").html(resposta);
            });
        });
        $('#select_artistas').on('tokenize:select', function (e, value) {
            $(this).tokenize2().trigger('tokenize:search', [$(this).tokenize2().input.val()]);
        });
        $('#select_musicas').tokenize2({
            searchFromStart: false,
            delimiter: ';', //,
            placeholder: 'Música' //false
        });
        $('#select_musicas').on('tokenize:tokens:add', function (e, value) {
            $.post('@Url.Action("AdicionarMusicaJquery", "Jquery")', { titulo_musica: value, model: "@Model.GetType().Name" },
            function (resposta) {
                $("#lista_musicas").html(resposta);
            });
        });
        $('#select_musicas').on('tokenize:tokens:remove', function (e, value) {
            $.post('@Url.Action("RemoverMusicaJquery", "Jquery")', { id: value, model: "@Model.GetType().Name" },
            function (resposta) {
                $("#lista_musicas").html(resposta);
            });
        });
        $('#select_tags').tokenize2({
            searchFromStart: false,
            delimiter: ';', //,
            placeholder: 'Tag' //false
        });
        $('#select_tags').on('tokenize:tokens:add', function (e, value) {
            $.post('@Url.Action("AdicionarTagJquery", "Jquery")', { nome_tag: value, model: "@Model.GetType().Name" },
            function (resposta) {
                $("#lista_tags").html(resposta);
            });
        });
        $('#select_tags').on('tokenize:tokens:remove', function (e, value) {
            $.post('@Url.Action("RemoverTagJquery", "Jquery")', { id: value, model: "@Model.GetType().Name" },
            function (resposta) {
                $("#lista_tags").html(resposta);
            });
        });
    });
</script>
<div class="modal-body row">
    <div class="col-md-12">
        @foreach (string thumb in Model.Thumbs) {
            <img class="img-rounded img-responsive form_thumb" src="~/capturas/@thumb">
        }
    </div>
    <div style="clear:both;margin-bottom:10px"></div>
    <form id="form_video">
        <div class="col-md-12" id="video_metadata">
            @Html.HiddenFor(model => Model.Id, new { @Value = Model.Id })
            @Html.HiddenFor(model => Model.Duracao, new { @Value = Model.Duracao })
            @Html.HiddenFor(model => Model.Resolucao, new { @Value = Model.Resolucao })
            @Html.HiddenFor(model => Model.FormatoVideo, new { @Value = Model.FormatoVideo })
            @Html.HiddenFor(model => Model.Fps, new { @Value = Model.Fps })
            @Html.HiddenFor(model => Model.CanaisAudio, new { @Value = Model.CanaisAudio })
            @Html.HiddenFor(model => Model.FormatoAudio, new { @Value = Model.FormatoAudio })
            Artista: @Model.ArtistaPrincipal, Duração: @Model.Duracao, Resolução: @Model.Resolucao, Formato Vídeo: @Model.FormatoVideo, FPS: @Model.Fps, Canais: @Model.CanaisAudio, Formato Áudio: @Model.FormatoAudio
            <button type="button" class="btn btn-default atualizar_metadata" aria-label="Atualizar">
                <span class="glyphicon glyphicon-refresh" aria-hidden="true"></span>
            </button>
        </div>
        <div class="col-md-12">
            <div class="form-group">
                @Html.LabelFor(model => model.Titulo, new { @style = "display:block" })
                @Html.TextBoxFor(model => model.Titulo, new { @class = "form-control input_titulo", @placeholder = "Título", @Value = Model.Titulo })
            </div>
        </div>
        <div class="col-md-2">
            <label for="artistas" style="display:block">Artistas</label>
            @Html.DropDownListFor(model => Model.ListaArtistas,
            new SelectList(Model.ListaArtistas, "id", "nome", Model.ListaArtistas), null, new { @id = "select_artistas", @class = "form-control campo_filtro", @multiple = true })
            <div id="lista_artistas" style="display: none"></div>
        </div>
        <div class="col-md-2">
            <label for="musicas" style="display:block">Musicas</label>
            @Html.DropDownListFor(model => Model.ListaMusicas,
            new SelectList(Model.ListaMusicas, "id", "titulo", Model.ListaMusicas), null, new { @id = "select_musicas", @class = "form-control campo_filtro", @multiple = true })
            <div id="lista_musicas" style="display: none">
                @for (int i = 0; i < Model.Musicas.Count; i++) {
                    <div>
                        @Html.HiddenFor(model => Model.Musicas[i].id, new { @Value = Model.Musicas[i].id })
                        <label>@Model.Musicas[i].titulo</label>
                    </div>
                }
            </div>
        </div>
        <div class="col-md-2">
            <label for="tags" style="display:block">Tags</label>
            @Html.DropDownListFor(model => Model.ListaTags,
            new SelectList(Model.ListaTags, "id", "nome", Model.ListaTags), null, new { @id = "select_tags", @class = "form-control campo_filtro", @multiple = true })
            <div id="lista_tags" style="display: none">
                @for (int i = 0; i < Model.Tags.Count; i++) {
                    <div>
                        @Html.HiddenFor(model => Model.Tags[i].id, new { @Value = Model.Tags[i].id })
                        <label>@Model.Tags[i].nome</label>
                    </div>
                }
            </div>
        </div>
    </form>
</div>