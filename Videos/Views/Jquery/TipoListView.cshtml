@model Videos.Models.ViewModel.VideosView
@for (int i = 0; i < Model.Tipos.Count; i++) {
    <div>
        @Html.HiddenFor(model => Model.Tipos[i].id, new { @Value = Model.Tipos[i].id })
        <label>@Model.Tipos[i].descricao</label>
    </div>
}